services:
  proxmox-mcp:
    build: .
    ports:
      - "8090:8090"
    env_file:
      - .env
    volumes:
      # Пробросить SSH-ключ если используете SSH-режим
      - ~/.ssh:/root/.ssh:ro
    restart: unless-stopped
